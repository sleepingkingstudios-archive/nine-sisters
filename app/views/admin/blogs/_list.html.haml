-# app/views/admin/blogs/_list.html.haml

- blogs ||= @blogs

- blogs.each do |blog|
  %h3= link_to blog.title, [:admin, blog]
  
  %p
    - if blog.summary
      = blog.summary
    - else
      = lighten("No Summary")
  
  %p= link_to "View All Posts", admin_blog_posts_path(blog), :class => :accent
  
  %h4{ :class => :"no-margin" } Recent Activity
  = render "recent_activity", :blog => blog